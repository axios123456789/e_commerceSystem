<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.e_commerce.manager.mapper.SysUserMapper">
    <sql id="columns">
        id,
        username userName,
        password,
        name,
        phone,
        avatar,
        description,
        status,
        create_time,
        update_time,
        is_deleted
    </sql>

<!--    添加用户-->
    <insert id="addSysUser">
        insert into sys_user(
            username,
            password,
            name,
            phone,
            avatar,
            description,
            status
        )values (
            #{userName},
            #{password},
            #{name},
            #{phone},
            #{avatar},
            #{description},
            #{status}
        )
    </insert>

<!--    修改用户-->
    <update id="updateSysUser">
        update sys_user
        set
        <if test="userName != null and userName != ''">
            username = #{userName},
        </if>
        <if test="password != null and password != ''">
            password = #{password},
        </if>
        <if test="name != null and name != ''">
            name = #{name},
        </if>
        <if test="phone != null and phone != ''">
            phone = #{phone},
        </if>
        <if test="description != null and description != ''">
            description = #{description},
        </if>
        <if test="avatar != null and avatar != ''">
            avatar = #{avatar},
        </if>
        <if test="status != null and status != ''">
            status = #{status},
        </if>
        update_time = now()
        where id = #{id}
    </update>

<!--    逻辑删除用户根据id-->
    <update id="deleteSysUserById">
        update sys_user
        set is_deleted = 1
        where id = #{userId}
    </update>

    <!--根据用户名查询用户信息-->
   <!-- <select id="getUserInfoByUserName" resultType="com.example.e_commerce.model.entity.system.SysUser">
        select <include refid="columns"/> from sys_user where username = #{userName} and is_deleted = 0
    </select>-->

<!--    条件查询用户信息-->
    <select id="findSysUserListByCondition" resultType="com.example.e_commerce.model.entity.system.SysUser">
        select <include refid="columns"/>
        from sys_user
        <where>
            <if test="keyword != null and keyword != ''">
                and username like concat('%', #{keyword}, '%')
            </if>
            <if test="createTimeBegin != null and createTimeBegin != ''">
                and create_time &gt;= #{createTimeBegin}
            </if>
            <if test="createTimeEnd != null and createTimeEnd != ''">
                and create_time &lt;= #{createTimeEnd}
            </if>
            and is_deleted = 0
        </where>
        order by id desc
    </select>

<!--    根据用户名查询用户-->
    <select id="getSysUserByUserName" resultType="com.example.e_commerce.model.entity.system.SysUser">
        select <include refid="columns"/>
        from sys_user
        where username = #{userName} and is_deleted = 0
    </select>
</mapper>