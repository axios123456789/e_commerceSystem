<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.e_commerce.product.mapper.ProductDetailsMapper">
    <sql id="columns">
        id,
        product_id,
        image_urls,
        create_time,
        update_time,
        is_deleted
    </sql>

<!--    根据product_id查询商品详细信息-->
    <select id="getByProductId" resultType="com.example.e_commerce.model.entity.product.ProductDetails">
        select <include refid="columns"/> from product_details
        where product_id = #{productId}
    </select>
</mapper>